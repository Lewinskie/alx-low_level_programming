#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: 0 if successful, non-zero otherwise
 */
int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c--)
	{
		putchar(x);
	}
	putchar('\n');
	return (0);
}
