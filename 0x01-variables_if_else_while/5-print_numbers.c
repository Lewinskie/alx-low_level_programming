#include <stdio.h>
/**
 * main - Entry point
 * Description - program that prints all single digit numbers of base10 starting from 0
 * Return: 0 if successful, non-zero otherwise
 */
int main(void)
{
	int x;

	for (x = 0; x < 10; x++)
	{
		puts(x);
	}
	return (0);
}
