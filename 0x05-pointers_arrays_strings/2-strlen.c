#include "main.h"

/**
 * _strlen - Returns strings length
 * @s: Pointer to the string
 * Return: string's length
 */
int _strlen(char *s)
{
	int length = 0;

	for (; s[length] != '\0'; length++)
	{
	}
	return (length);
}
